version: '3'

services:

  database:
    container_name: IMDb
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: IMDb
      MYSQL_USER: user
      MYSQL_PASSWORD: password
      MYSQL_ROOT_PASSWORD: password
    ports:
      - '3306:3306'